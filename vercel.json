{
  "buildCommand": "cd apps/web && pnpm run build && cd ../.. && mkdir -p dist && cp -r apps/web/dist/. dist/",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

